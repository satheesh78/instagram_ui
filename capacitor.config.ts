import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.clone.instagram',
  appName: 'Instagram Clone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
